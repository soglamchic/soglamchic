{% extends 'base.html.twig' %}

{% block title %}CommandeDetail{% endblock %}

{% block body %}
    <h1>CommandeDetail</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ commande_detail.id }}</td>
            </tr>
            <tr>
                <th>Quantite</th>
                <td>{{ commande_detail.quantite }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('admin_commande_detail_index') }}">back to list</a>

    <a href="{{ path('admin_commande_detail_edit', {'id': commande_detail.id}) }}">edit</a>

    {{ include('admin_commande_detail/_delete_form.html.twig') }}
{% endblock %}
