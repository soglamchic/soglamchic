<form method="post" action="{{ path('admin_commande_detail_delete', {'id': commande_detail.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ commande_detail.id) }}">
    <button class="btn">Delete</button>
</form>
